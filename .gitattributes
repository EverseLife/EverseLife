# LF everywhere -- as in the vault. Otherwise the diff consists of line endings, not code.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary

# The AGPL text is verbatim by definition -- no normalisation of any kind.
LICENSE -text
