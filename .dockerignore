# The server image build context is the repository root (`vault/` is needed).
# Everything that is neither server code nor the vault snapshot must not go here.

.git
.github
.idea
.vscode
.jbeval
.claude
.pytest_cache
.ruff_cache
.mypy_cache

# The client is built by its own image, from its own context.
frontend

backend/.venv
backend/.env
backend/tests
backend/.pytest_cache
backend/.ruff_cache
**/__pycache__
**/*.py[cod]

deploy
README.md
DEPLOY.md
docker-compose.yml
