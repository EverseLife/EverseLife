# Path to the game-design vault's build/ directory. The engine reads only it.
EVERSELIFE_VAULT_BUILD=../../everselife-vault/build

EVERSELIFE_DATABASE_URL=postgresql+asyncpg://everselife:everselife@localhost:5432/everselife
EVERSELIFE_REDIS_URL=redis://localhost:6379/0

# How many journal jobs are taken per worker pass.
EVERSELIFE_JOB_BATCH=64

# Database connections this process keeps, and how many more at a peak. One
# developer needs no more than the default; production raises both and runs
# the server in several processes (`deploy/.env.example`).
EVERSELIFE_DB_POOL_SIZE=5
EVERSELIFE_DB_MAX_OVERFLOW=10

# The revision this build came from -- `/public/source` reports it, because
# AGPL §13 is about the source of *this* version, not of the repository in
# general. Empty locally; in production it is `github.sha`.
# EVERSELIFE_RELEASE=

EVERSELIFE_LOG_LEVEL=INFO

# Whether `readonly=True` on a socket command is a check or a declaration
# (`src/db/readonly.py`): `raise` stops a read that writes, `warn` lets it
# answer and names it in the log, `off` takes the listeners away. The default
# is `raise` -- on a developer's copy a read that writes is a defect to see at
# once, not later; production sets `warn` (`deploy/compose.yaml`).
# EVERSELIFE_READONLY_GUARD=raise
