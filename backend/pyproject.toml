[project]
name = "everselife-server"
version = "0.0.1"
description = "everse.life game server"
license = "AGPL-3.0-only"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "redis>=5.2",
    "arq>=0.26",
    "orjson>=3.10",
    # Device fee: the server verifies the same estimate the client computes (D-112).
    "argon2-cffi>=23.1",
    "fluent-runtime>=0.4",
    # The sky is integrated, not tabled (D-289): forty variants of a passage go
    # through the integrator as one matrix.
    "numpy>=2.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.8",
    # `pytest -n 8`: the suite is a run against a database, and the database is
    # the wait. Each worker gets a database of its own (see tests/conftest.py).
    "pytest-xdist>=3.6",
    # Layers `api -> engine -> models -> constants` and no import cycles: checked,
    # not hoped for (review 2026-08-23).
    "import-linter>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"
# One event loop for the whole run instead of one per test. Without it the
# engine could not be shared between tests: an asyncpg connection belongs to the
# loop that opened it, so every test would pay some seventy milliseconds to
# connect again -- several times over.
asyncio_default_test_loop_scope = "session"
asyncio_default_fixture_loop_scope = "session"
filterwarnings = ["error::DeprecationWarning:src.*"]

[tool.ruff]
line-length = 100
src = [".", "tests"]
# The migration body is generated by Alembic -- reformatting it means diverging
# from what will be generated next time.

# `.claude/worktrees/<name>/` is a whole scratch copy of the repository that
# a parallel session works in. Walking into one means linting somebody
# else's half-finished edit, and reporting it as this tree's problem.
extend-exclude = ["migrations/versions", ".claude/worktrees"]

[tool.ruff.lint]
#: PLC0415 -- an import inside a function hides a cycle (348 of them before
#: the review of 2026-08-23). The ones that remain are marked `noqa` with
#: the cycle they break; a new one needs the same justification.
select = ["E", "F", "I", "UP", "B", "SIM", "PLC0415"]

[tool.ruff.lint.per-file-ignores]
#: Tests import what they need where they need it; the cycle rule is for `src`.
"tests/**" = ["PLC0415"]
"migrations/**" = ["PLC0415"]
"tools/**" = ["PLC0415"]

[tool.importlinter]
root_package = "src"
# The review of 2026-08-23 counted 348 lazy imports and 29 mutual cycles in
# `src.engine`. The layers contract is enforced now; the cycle contract for the
# engine is the goal of wave 3 and stays commented until the cycles are broken.
[[tool.importlinter.contracts]]
name = "Layers: api -> engine -> sky -> models -> constants -> i18n"
type = "layers"
layers = ["src.api", "src.engine", "src.sky", "src.models", "src.constants", "src.i18n"]

# The city, cut out of a 1084-line `polity.py` that held founding, offices,
# code-laws and citizenship at once and let all four reach into each other.
# The cut only means something if it cannot silently grow back: without this
# the stack lives in four module docstrings, and a docstring does not fail a
# build.
#
# `forbidden` and not `layers`, and `allow_indirect_imports` on purpose. The
# package still sits inside two cycles it did not create -- `city -> travel ->
# city` and `city -> vote -> city`, both wave-3 leftovers -- so every pair of
# city modules is reachable from every other the long way round, and a layers
# contract would fail on chains that have nothing to do with this cut. What is
# checked here is the thing the cut actually established: which module may name
# which **directly**. Turn these into a layers contract on the day the engine's
# own cycles are gone.
# The ship's sky (D-289): `sim` owns the state and is the floor; the tick and
# the fates (`helm`), the meeting (`meet`), what is seen (`sighting`) and the
# order (`crossing`) stand on it and it names none of them.
[[tool.importlinter.contracts]]
name = "The ship's sky is a stack, and sim is its floor"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["src.engine.ship.sim"]
forbidden_modules = [
    "src.engine.ship.helm",
    "src.engine.ship.hold",
    "src.engine.ship.fate",
    "src.engine.ship.meet",
    "src.engine.ship.sighting",
    "src.engine.ship.crossing",
]

[[tool.importlinter.contracts]]
name = "The ship's fates sit under the hold and the helm"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["src.engine.ship.fate"]
forbidden_modules = [
    "src.engine.ship.hold",
    "src.engine.ship.helm",
    "src.engine.ship.meet",
    "src.engine.ship.crossing",
]

[[tool.importlinter.contracts]]
name = "The ship's hold sits under the helm"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["src.engine.ship.hold"]
forbidden_modules = [
    "src.engine.ship.helm",
    "src.engine.ship.meet",
    "src.engine.ship.crossing",
]

[[tool.importlinter.contracts]]
name = "The city is a stack, and its floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.city.hall"]
forbidden_modules = [
    "src.engine.city.office",
    "src.engine.city.law",
    "src.engine.city.citizen",
    "src.engine.city.land",
    "src.engine.city.founding",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Offices know nothing of laws, citizens or founding"
type = "forbidden"
source_modules = ["src.engine.city.office"]
forbidden_modules = [
    "src.engine.city.law",
    "src.engine.city.citizen",
    "src.engine.city.land",
    "src.engine.city.founding",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "A law does not ask who is a citizen"
type = "forbidden"
source_modules = ["src.engine.city.law"]
forbidden_modules = [
    "src.engine.city.citizen",
    "src.engine.city.land",
    "src.engine.city.founding",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Citizenship and land come before founding, not after"
type = "forbidden"
source_modules = ["src.engine.city.citizen", "src.engine.city.land"]
forbidden_modules = ["src.engine.city.founding"]
allow_indirect_imports = "true"

# The market, cut out of a 1636-line `market.py` that held the vocabulary, the
# terminal cells, the matching, the entrances and the book reading at once.
# Same shape as the city contracts, and `forbidden` for the same reason: the
# market sits inside engine cycles it did not create -- `craft` reaches back
# into it lazily, and its own door to `frost` is lazy -- so a layers contract
# would fail on chains that have nothing to do with this cut. The stack it
# pins directly: deal -> match -> counter -> _base, window -> _base.
[[tool.importlinter.contracts]]
name = "The market's vocabulary asks nobody"
type = "forbidden"
source_modules = ["src.engine.market._base"]
forbidden_modules = [
    "src.engine.market.counter",
    "src.engine.market.match",
    "src.engine.market.deal",
    "src.engine.market.window",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The counter moves stacks and knows nothing of orders crossing"
type = "forbidden"
source_modules = ["src.engine.market.counter"]
forbidden_modules = [
    "src.engine.market.match",
    "src.engine.market.deal",
    "src.engine.market.window",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Matching serves the entrances, never calls them"
type = "forbidden"
source_modules = ["src.engine.market.match"]
forbidden_modules = ["src.engine.market.deal", "src.engine.market.window"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The shop window only reads"
type = "forbidden"
source_modules = ["src.engine.market.window"]
forbidden_modules = [
    "src.engine.market.counter",
    "src.engine.market.match",
    "src.engine.market.deal",
]
allow_indirect_imports = "true"

# The face, cut out of a 931-line `mining.py` that held the formulas, the
# shift commands and the collapse at once. Same shape as the market contracts,
# and `forbidden` for the same reason: mining sits inside engine cycles it did
# not create -- `death` reaches back into it lazily, `rig` borrows its
# depletion rule lazily -- so a layers contract would fail on chains that have
# nothing to do with this cut. The stack it pins directly:
# face -> collapse -> _base.
[[tool.importlinter.contracts]]
name = "The mining vocabulary asks nobody"
type = "forbidden"
source_modules = ["src.engine.mining._base"]
forbidden_modules = [
    "src.engine.mining.face",
    "src.engine.mining.collapse",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "A collapse ends a shift and never opens one"
type = "forbidden"
source_modules = ["src.engine.mining.collapse"]
forbidden_modules = ["src.engine.mining.face"]
allow_indirect_imports = "true"

# The eruptions, cut out of an 814-line `plates.py` that held the job chain,
# the fire, the map redraw and the vein moves at once. Same shape and same
# `forbidden` reasoning as the city and the market: plates sits inside engine
# cycles it did not create (`veins` reaches into `mining` lazily, `ways` into
# `death`, and both reach back), so a layers contract would fail on chains
# that have nothing to do with this cut. The stack it pins directly:
# clock -> {fire, ways, veins} -> _base, with `ways` allowed one door into
# `fire` for `_consume` -- the rift under a walker burns the pocket by the
# same rule the fields burn by.
[[tool.importlinter.contracts]]
name = "The planet's floor asks nobody above it"
type = "forbidden"
source_modules = ["src.engine.plates._base"]
forbidden_modules = [
    "src.engine.plates.clock",
    "src.engine.plates.fire",
    "src.engine.plates.ways",
    "src.engine.plates.veins",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The fire knows nothing of the clock, the ways or the veins"
type = "forbidden"
source_modules = ["src.engine.plates.fire"]
forbidden_modules = [
    "src.engine.plates.clock",
    "src.engine.plates.ways",
    "src.engine.plates.veins",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The ways burn through the fire's one door and never ring the clock"
type = "forbidden"
source_modules = ["src.engine.plates.ways"]
forbidden_modules = ["src.engine.plates.clock", "src.engine.plates.veins"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The veins move on the floor alone"
type = "forbidden"
source_modules = ["src.engine.plates.veins"]
forbidden_modules = [
    "src.engine.plates.clock",
    "src.engine.plates.fire",
    "src.engine.plates.ways",
]
allow_indirect_imports = "true"

# Six more packages cut on 2026-09-01 -- bank, travel, vote, oxygen, net,
# frost -- same `forbidden` shape as the city, the market and the plates, and
# for the same reason: they sit inside engine cycles they did not create, so
# a layers contract would fail on chains that have nothing to do with the
# cuts. What is pinned is what each cut established: which room may name
# which directly. The floors (`_base`) ask nobody.
[[tool.importlinter.contracts]]
name = "The bank's floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.bank._base"]
forbidden_modules = [
    "src.engine.bank.rate",
    "src.engine.bank.trust",
    "src.engine.bank.line",
    "src.engine.bank.council",
    "src.engine.bank.loan",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The rate, the trust and the line stand apart from the entrances"
type = "forbidden"
source_modules = [
    "src.engine.bank.rate",
    "src.engine.bank.trust",
    "src.engine.bank.line",
]
forbidden_modules = ["src.engine.bank.loan", "src.engine.bank.council"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The council moves the rate and never a loan"
type = "forbidden"
source_modules = ["src.engine.bank.council"]
forbidden_modules = [
    "src.engine.bank.loan",
    "src.engine.bank.line",
    "src.engine.bank.trust",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "A loan is priced by the line and the trust, never by the council"
type = "forbidden"
source_modules = ["src.engine.bank.loan"]
forbidden_modules = ["src.engine.bank.council", "src.engine.bank.rate"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The road's floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.travel._base"]
forbidden_modules = ["src.engine.travel.map", "src.engine.travel.walk"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The map and the walk do not name each other"
type = "forbidden"
source_modules = ["src.engine.travel.map"]
forbidden_modules = ["src.engine.travel.walk"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The walk crosses the map's edges without calling its builders"
type = "forbidden"
source_modules = ["src.engine.travel.walk"]
forbidden_modules = ["src.engine.travel.map"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The vote's floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.vote._base"]
forbidden_modules = ["src.engine.vote.council", "src.engine.vote.poll"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The council is a body, not a ballot box"
type = "forbidden"
source_modules = ["src.engine.vote.council"]
forbidden_modules = ["src.engine.vote.poll"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The airless floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.oxygen._base"]
forbidden_modules = [
    "src.engine.oxygen.supply",
    "src.engine.oxygen.breath",
    "src.engine.oxygen.gauge",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The stores know nothing of breathing or the gauge"
type = "forbidden"
source_modules = ["src.engine.oxygen.supply"]
forbidden_modules = ["src.engine.oxygen.breath", "src.engine.oxygen.gauge"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Breathing does not read the gauge, the gauge does not breathe"
type = "forbidden"
source_modules = ["src.engine.oxygen.breath"]
forbidden_modules = ["src.engine.oxygen.gauge"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The gauge only reads"
type = "forbidden"
source_modules = ["src.engine.oxygen.gauge"]
forbidden_modules = ["src.engine.oxygen.breath"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The Net's floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.net._base"]
forbidden_modules = [
    "src.engine.net.road",
    "src.engine.net.letters",
    "src.engine.net.channels",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The road carries letters and posts without reading either"
type = "forbidden"
source_modules = ["src.engine.net.road"]
forbidden_modules = ["src.engine.net.letters", "src.engine.net.channels"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Letters and channels do not name each other"
type = "forbidden"
source_modules = ["src.engine.net.letters"]
forbidden_modules = ["src.engine.net.channels"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Channels and letters do not name each other"
type = "forbidden"
source_modules = ["src.engine.net.channels"]
forbidden_modules = ["src.engine.net.letters"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The climate's floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.frost._base"]
forbidden_modules = [
    "src.engine.frost.warmth",
    "src.engine.frost.body",
    "src.engine.frost.hours",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Warmth belongs to places, not to bodies or the tick"
type = "forbidden"
source_modules = ["src.engine.frost.warmth"]
forbidden_modules = ["src.engine.frost.body", "src.engine.frost.hours"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The body's reserve never rings the world's hours"
type = "forbidden"
source_modules = ["src.engine.frost.body"]
forbidden_modules = ["src.engine.frost.hours"]
allow_indirect_imports = "true"

# Seven more cuts of 2026-09-02 -- push, estate.building, farm, craft.batch,
# automat, ship.view, works_city -- pinned the same way: which room may name
# which directly, floors ask nobody.
[[tool.importlinter.contracts]]
name = "The push floor asks nobody, and the pump never reads its tellers"
type = "forbidden"
source_modules = ["src.api.push._base", "src.api.push.pump"]
forbidden_modules = ["src.api.push.tellers"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The push vocabulary asks nobody"
type = "forbidden"
source_modules = ["src.api.push._base"]
forbidden_modules = ["src.api.push.pump"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The frame is read before anything is raised on it"
type = "forbidden"
source_modules = ["src.engine.estate.building.frame"]
forbidden_modules = ["src.engine.estate.building.build"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The field's floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.farm._base"]
forbidden_modules = ["src.engine.farm.plot", "src.engine.farm.season", "src.engine.farm.settle", "src.engine.farm.care", "src.engine.farm.text", "src.engine.farm.life"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The plot and the season do not name each other"
type = "forbidden"
source_modules = ["src.engine.farm.plot"]
forbidden_modules = ["src.engine.farm.season"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The season shapes no plots and does no care"
type = "forbidden"
source_modules = ["src.engine.farm.season"]
forbidden_modules = ["src.engine.farm.plot", "src.engine.farm.care", "src.engine.farm.text"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The life of a bed asks nobody in the field"
type = "forbidden"
source_modules = ["src.engine.farm.life"]
forbidden_modules = ["src.engine.farm._base", "src.engine.farm.plot", "src.engine.farm.settle", "src.engine.farm.care", "src.engine.farm.season", "src.engine.farm.text"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The bed's clock runs no action and no season"
type = "forbidden"
source_modules = ["src.engine.farm.settle"]
forbidden_modules = ["src.engine.farm.care", "src.engine.farm.season", "src.engine.farm.plot", "src.engine.farm.text"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The care and the season do not name each other"
type = "forbidden"
source_modules = ["src.engine.farm.care"]
forbidden_modules = ["src.engine.farm.season", "src.engine.farm.plot", "src.engine.farm.text"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The words of care shape no plots and run no season"
type = "forbidden"
source_modules = ["src.engine.farm.text"]
forbidden_modules = ["src.engine.farm.plot", "src.engine.farm.season"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The work at the bench never calls its own finisher or the shelf"
type = "forbidden"
source_modules = ["src.engine.craft.batch.work"]
forbidden_modules = ["src.engine.craft.batch.finish", "src.engine.craft.batch.copy"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Copying a recipe is not batch work"
type = "forbidden"
source_modules = ["src.engine.craft.batch.copy"]
forbidden_modules = ["src.engine.craft.batch.work", "src.engine.craft.batch.finish"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The automat's floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.automat._base"]
forbidden_modules = [
    "src.engine.automat.wire",
    "src.engine.automat.run",
    "src.engine.automat.board",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The wires know nothing of running or the board"
type = "forbidden"
source_modules = ["src.engine.automat.wire"]
forbidden_modules = ["src.engine.automat.run", "src.engine.automat.board"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The run never reads the operator's board"
type = "forbidden"
source_modules = ["src.engine.automat.run"]
forbidden_modules = ["src.engine.automat.board"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The sight and the sky do not name each other; only the card reads both"
type = "forbidden"
source_modules = ["src.engine.ship.view.sight"]
forbidden_modules = ["src.engine.ship.view.sky", "src.engine.ship.view.card"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The sky reads no sight and no card"
type = "forbidden"
source_modules = ["src.engine.ship.view.sky"]
forbidden_modules = ["src.engine.ship.view.sight", "src.engine.ship.view.card"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The city order's floor asks nobody"
type = "forbidden"
source_modules = ["src.engine.works_city._base"]
forbidden_modules = [
    "src.engine.works_city.order",
    "src.engine.works_city.pay",
    "src.engine.works_city.credit",
]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "Posting, paying and borrowing for works stand apart"
type = "forbidden"
source_modules = ["src.engine.works_city.order"]
forbidden_modules = ["src.engine.works_city.pay", "src.engine.works_city.credit"]
allow_indirect_imports = "true"

[[tool.importlinter.contracts]]
name = "The settlement posts nothing and borrows nothing"
type = "forbidden"
source_modules = ["src.engine.works_city.pay"]
forbidden_modules = ["src.engine.works_city.order", "src.engine.works_city.credit"]
allow_indirect_imports = "true"
